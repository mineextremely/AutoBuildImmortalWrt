name: build-N1
on:
  workflow_dispatch:
    inputs:
      openwrt_kernel:
        description: "选择构建的内核(斐讯N1)"
        required: false
        default: "6.6.93"
        type: choice
        options:
          - 5.4.291
          - 5.10.235
          - 5.15.189
          - 6.1.121
          - 6.1.122
          - 6.1.123
          - 6.1.124
          - 6.1.126
          - 6.1.127
          - 6.1.128
          - 6.1.129
          - 6.1.130
          - 6.1.131
          - 6.1.132
          - 6.1.133
          - 6.1.134
          - 6.1.135
          - 6.1.136
          - 6.1.138
          - 6.1.139
          - 6.1.140
          - 6.1.141
          - 6.1.142
          - 6.1.144
          - 6.1.146
          - 6.6.67
          - 6.6.68
          - 6.6.69
          - 6.6.70
          - 6.6.71
          - 6.6.72
          - 6.6.74
          - 6.6.76
          - 6.6.79
          - 6.6.80
          - 6.6.81
          - 6.6.83
          - 6.6.84
          - 6.6.85
          - 6.6.86
          - 6.6.87
          - 6.6.88
          - 6.6.89
          - 6.6.90
          - 6.6.91
          - 6.6.92
          - 6.6.93
          - 6.6.94
          - 6.6.95
          - 6.6.97
          - 6.6.99
          - 6.12.16
          - 6.12.17
          - 6.12.18
          - 6.12.19
          - 6.12.20
          - 6.12.21
          - 6.12.22
          - 6.12.23
          - 6.12.24
          - 6.12.25
          - 6.12.26
          - 6.12.28
          - 6.12.29
          - 6.12.30
          - 6.12.31
          - 6.12.32
          - 6.12.33
          - 6.12.34
          - 6.12.35
          - 6.12.37
          - 6.12.39
          
      include_docker:
        description: |
          是否编译 Docker 插件
        required: true
        default: 'no'
        type: choice
        options:
          - 'yes'
          - 'no'
      enable_store:
        description: "集成 store 商店"
        required: false
        type: boolean
        default: true
      skip_imagebuilder:
        description: "直接使用底包快速构建固件, 忽略自定义插件 ----（跳过 ImageBuilder）"
        required: false
        default: false
        type: boolean
      # 新增一个输入参数，用于自定义 rootfs 的 URL
      custom_rootfs_url:
        description: |
          当‘跳过 ImageBuilder’为 true 时，可在此处填写自定义的 rootfs.tar.gz 文件 URL。
          若留空，则使用默认地址。
        required: false
        default: ""
        type: string
      # 新增参数
      custom_firmware_name:
        description: |
          当使用自定义 rootfs URL 时，可在此处设置固件名称前缀（替换 immortalwrt-24.10.3）
          若留空，则使用默认名称。
        required: false
        default: ""
        type: string
      replace_banner:
        description: '替换为ImmortalWrt的banner信息'
        required: false
        default: false
        type: boolean
      rootfs_partsize:
        description: '软件包大小1G固定'
        required: true
        default: "1024"
        type: choice
        options:
          - '1024'
      builder_name:
        description: "设置打包作者的名称"
        required: true
        default: 'MINEX'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set executable permissions
        run: chmod +x ${{ github.workspace }}/n1/build.sh

      - name: Debug workspace contents
        run: ls -R
      
      - name: Enable Store integration
        run: |
          if [ "${{ github.event.inputs.enable_store }}" == "true" ]; then
            echo 'CUSTOM_PACKAGES="$CUSTOM_PACKAGES luci-app-store"' >> shell/custom-packages.sh
            echo "✅ 已追加 luci-app-store"
          else
            echo "❎ 未启用 luci-app-store"
          fi

      - name: 处理是否替换 banner 信息
        if: ${{ github.event.inputs.skip_imagebuilder == 'false' && github.event.inputs.replace_banner == 'true' }}
        run: |
          # 使用ImageBuilder构建 且用户勾选了替换banner时
          cp n1/99-banner.sh files/etc/uci-defaults/
      
      - name: Building N1 ImmortalWrt rootfs
        if: ${{ github.event.inputs.skip_imagebuilder != 'true' }}
        run: |
          echo "✅ 使用 ImageBuilder 构建 rootfs"
          profiles="generic"
          include_docker="${{ github.event.inputs.include_docker }}"
          rootfs_partsize="${{ github.event.inputs.rootfs_partsize }}"
          
          IFS=',' read -r -a profile_array <<< "$profiles"
          
          for profile in "${profile_array[@]}"; do
            echo "Building for profile: $profile"
            
            docker run --rm -i \
              --user root \
              -v "${{ github.workspace }}/bin:/home/build/immortalwrt/bin" \
              -v "${{ github.workspace }}/files/etc/uci-defaults:/home/build/immortalwrt/files/etc/uci-defaults" \
              -v "${{ github.workspace }}/arch/arch.conf:/home/build/immortalwrt/files/etc/opkg/arch.conf" \
              -v "${{ github.workspace }}/shell:/home/build/immortalwrt/shell" \
              -v "${{ github.workspace }}/n1/banner:/home/build/immortalwrt/files/mnt/banner" \
              -v "${{ github.workspace }}/n1/imm.config:/home/build/immortalwrt/.config" \
              -v "${{ github.workspace }}/n1/build.sh:/home/build/immortalwrt/build.sh" \
              -e PROFILE=$profile \
              -e INCLUDE_DOCKER=$include_docker \
              -e ROOTFS_PARTSIZE=$rootfs_partsize \
              immortalwrt/imagebuilder:armsr-armv8-openwrt-24.10.3 /bin/bash /home/build/immortalwrt/build.sh
          done
          
      - name: Download prebuilt rootfs.tar.gz (使用自定义或默认URL)
        if: ${{ github.event.inputs.skip_imagebuilder == 'true' }}
        run: |
          echo "📦 正在下载预构建 rootfs"
          mkdir -p bin/targets/armsr/armv8
          
          # 判断是否提供了自定义 URL
          CUSTOM_URL="${{ github.event.inputs.custom_rootfs_url }}"
          if [ -n "$CUSTOM_URL" ]; then
            echo "✅ 使用用户自定义的 rootfs URL: $CUSTOM_URL"
            ROOTFS_URL="$CUSTOM_URL"
          else
            # 这是你原来的默认地址
            DEFAULT_URL="https://github.com/wukongdaily/AutoBuildImmortalWrt/releases/download/rootfs/immortalwrt-24.10.2-armsr-armv8-generic-rootfs.tar.gz"
            echo "ℹ️ 使用默认的 rootfs URL: $DEFAULT_URL"
            ROOTFS_URL="$DEFAULT_URL"
          fi
          
          # 使用 curl 下载文件，并指定输出路径和文件名
          curl -L -o bin/targets/armsr/armv8/openwrt-armvirt-64-default-rootfs.tar.gz "$ROOTFS_URL"
          
          # 可选：检查下载是否成功
          if [ $? -eq 0 ]; then
            echo "✅ rootfs 下载成功"
          else
            echo "❌ rootfs 下载失败，请检查 URL 是否正确且可访问"
            exit 1
          fi

      - name: 查找rootfs.tar.gz所在路径
        id: find_rootfs
        run: |
          ROOTFS_FILE=$(find bin/targets/armsr/armv8/ -type f -name "*rootfs.tar.gz" | head -n1)
          echo "✅ Found: $ROOTFS_FILE"
          if [ ! -f "$ROOTFS_FILE" ]; then
            echo "❌ 找不到 rootfs.tar.gz 文件"
            exit 1
          fi
          echo "file=$ROOTFS_FILE" >> $GITHUB_OUTPUT

      - name: Package armsr-armv8 as ImmortalWrt for N1
        uses: wukongdaily/flippy-openwrt-actions@master
        if: ${{ steps.down.outputs.status }} == 'success' && !cancelled()
        env:
          OPENWRT_ARMVIRT: ${{ steps.find_rootfs.outputs.file }}
          PACKAGE_SOC: s905d
          KERNEL_VERSION_NAME: ${{ inputs.openwrt_kernel }}
          KERNEL_AUTO_LATEST: false
          WHOAMI: ${{ inputs.builder_name }}

      - name: Rename .img.gz files
        id: rename
        run: |
          MODEL=${{ github.event.inputs.openwrt_board }}
          # 检查是否使用了自定义 rootfs URL
          CUSTOM_ROOTFS_URL="${{ github.event.inputs.custom_rootfs_url }}"
          SKIP_IMAGEBUILDER=${{ github.event.inputs.skip_imagebuilder }}
          
          if [ -n "$CUSTOM_ROOTFS_URL" ] && [ "$SKIP_IMAGEBUILDER" = "true" ]; then
            # 使用自定义 rootfs URL，检查是否有自定义固件名称
            CUSTOM_NAME="${{ github.event.inputs.custom_firmware_name }}"
            if [ -n "$CUSTOM_NAME" ]; then
              PREFIX="$CUSTOM_NAME"
              echo "✅ 使用自定义固件名称: $PREFIX"
            else
              PREFIX="immortalwrt-24.10.3"
              echo "ℹ️ 使用默认固件名称"
            fi
          else
            PREFIX="immortalwrt-24.10.2"
            echo "ℹ️ 使用默认固件名称"
          fi


      - name: Upload firmware to GitHub Releases
        uses: softprops/action-gh-release@v2.2.1
        with:
          tag_name: phicomm-n1
          name: ImmortalWrt for Phicomm N1
          body_path: ${{ github.workspace }}/n1/info.md
          files: |
            ${{ env.PACKAGED_OUTPUTPATH }}/*.img.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
